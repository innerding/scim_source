# Handover — SCIM3 / MVP-Lichtenberg (Stand 2026-05-31)

*Für den nächsten Chat. Ohne Umriss — P0 wird im laufenden Chat fertiggestellt; der nächste Chat startet bei P1.*

## Projekt & Setup
- **Repo (Haupt-Checkout, hier arbeiten):** `/Users/dietmarbroda/SCIM3ClaudeMax/scim_source`
- **Stack:** React + TS + Vite. Deploy: **Cloudflare Pages auto bei Push auf `main`** (~60–90 s). URL **scim3.diesenpark.com**, Login-Code `operator`. Worker (`worker/`) separat via `wrangler deploy`. User testet auf **Safari**.

## Workflow-Regeln (KRITISCH)
- **Im Haupt-Checkout arbeiten**; Bash-cwd driftet zum Worktree → **immer `cd /Users/dietmarbroda/SCIM3ClaudeMax/scim_source &&` voranstellen.**
- **Direkt auf `main` committen + pushen** (kein Worktree, kein PR). Bei Reject: `git pull --rebase origin main`, dann push.
- **Nach JEDEM Commit den GitHub-Actions-Deploy beobachten** (`gh run list --branch main`) bis grün, dem User Bescheid geben.
- Commit-Messages enden mit `Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>`.
- Build-Check: `npm run build` (tsc, `noUnusedLocals`).

## Stand: Wegnetz-Tool-Block im Drawer FERTIG
DrawerPanel (Wegnetz-Tab): **Tool-Header-Leiste** (Registry `drawerTools`, tab-gefiltert: Umriss links / Snap mittig / Wegnetz rechts) + linke **Drop-Panels** (Filter + Profi, durchklickbarer Collapse) + **Footer** (grün: Σ Netz/Wanderweg/Asphalt + KB; gelb: Maskierung/POI-Anker; Reduktions-Regler).
Werkzeuge rechts: **Netz-Schwelle · Sackgassen(rot)+blau(abgeschnitten) · Verbinden(gapTol) · OSM-Wege(A→B Asphalt-Zwei-Punkt) · 🗑 Entfernen · 🔗 POI-Connect · Anwenden(roter Puls)**. Karten-Modi (OSM/Entfernen/POI-Connect) schließen sich gegenseitig aus. Footer-Reduktion: **DP (topologie-sicheres Douglas-Peucker) + Koord-Stellen**.
Engine in `regio-content`: `netGraph.ts` (graphCompose=Noding, bridgeGaps, filterEdges, simplifyNet, classifyComponents) · `pathEngine.ts` (deriveWanderwegnetz, anchorPois, isAsphalt, netMeters/asphaltMeters, roundPolyline, connectorPieceBetween, cropNetToMask) · `pathConfig.ts` (Defaults Neben-/Landstraße 500/250).

> **P0 Umriss** wird im laufenden Chat fertig (siehe `docs/umriss_todo.md`, UÖ1–UÖ7) → nächster Chat startet bei **P1**.

## Pläne/Docs (autoritativ, im Repo)
- `docs/fahrplan_mvp_lichtenberg.md` — **P0→P5** Panel-/Modul-Kette bis testbare MVP.
- `docs/umbauplan_representation_telco_inspector.md` — S1–S4 + **M1–M4** (inkl. R2/Paket).
- `docs/umriss_todo.md` — UÖ1–UÖ7.
- `docs/umbauplan_drawer_netz_engine.md`, `docs/drawer_werkzeugleiste_spec.md`.

## Memories (Design-Konsens)
`project_wegnetz_ziel` (Nordstern: sackgassenfrei, degree-1 = nur POIs) · `project_colour_mesh` (Mesh wird ausgeliefert; per-Strecke gleiche Teilung; statische Geometrie + volatiles Load-Array; Operator-seitig erzeugt) · `project_datengroesse_hebel` (DP > Stellen; 0,33-m-Budget teilen) · `project_netz_normalisierung` (E5: Backen vor Commit, Resample/ids nach Commit) · `project_asphalt_tracking` · `project_runtime_mvp` · `project_poi_stable_id_token`.

## Nächste Schritte (ab P1, ohne Umriss)
1. **P1 Backen + Commit** (= M1 gültige Representation): `DrawerPanel.onSave/buildNet` bäckt das Netz (Brücken+Stücke real, blau/exkludiert raus, POIs re-anchoren, Sackgassen prunen, DP+Stellen final) → `WorkspacePanel.onCommitDraft` schreibt `data/wegnetze/…json` + setzt **`Representation.wegnetz_id`**. Worker-Whitelist deckt `data/wegnetze/` + `data/representations/` bereits ab.
2. **P2 Resampler** (`regio-content`, neue reine Funktion z. B. `resampleMesh`): committetes Netz → regelmäßige Segmente (per-Strecke gleiche Teilung) + **stabile, geometrie-verankerte Segment-ids**; Aufruf in einem Represent-Build-Schritt **nach** dem Workspace.
3. **P3 Telco-Sim → Load-Array** (Transmitter = **P06**, `P06SimulationForm` → `TelcoLoadState`/`normalized_load_score`): Sim-Werte auf Segment-ids mappen → volatiles Load-Array (id→Wert). **P04** = Real-Eingang (nicht die Sim); `telco-load` ist nur die Datenform.
4. **P4 Inspector echt** (`ScimMap.tsx` + `colourMeshOverlay.ts`): resampeltes Mesh nach Load-Array färben statt `generateHotspots`+Overpass. **`edgeTypeConfig` (P02-Kantentypen) retire** (vom Drawer-`pathConfig` abgelöst, aber noch LIVE — erst hier entfernen). P01-`svg_overlay.excluded_edge_types`-Zweck klären.
5. **P5 Runtime-App-Shell** (`src/runtime/*`, neu): Lichtenberg-Representation + Mesh + Load **lokal** (noch ohne R2) → Karte/POIs/Routen-UI (Wishlist→Route→Guidance)/BCK-BAK/Telco-Sim-Button = **testbare MVP**.
- **Danach M3:** Sensus-Core-Paket (P11) → **R2-Publish** (für „verschiedene Pakete/Laufzeiten").

## Offene Entscheidungen
- **Segment-id-Schema** deterministisch + stabil über Re-Commits (sonst driftet das Telco-Mapping — Prinzip wie POI-Stable-Token).
- **Wo lebt das Load-Array** (volatil, getrennt von Geometrie, 5-Min-Zyklus).
- **P01 SVG-Overlay** behalten (Begehbarkeit) oder retire?

## Technische Fallstricke
- **Geoman-Weißer-Screen:** kein globales `disable*` über programmatische Polygone → `removeControls()` + gezieltes `layer.pm.disable()`.
- **localStorage:** OSM-Edge-Cache wird beschnitten (`pruneOsmEdgeCache`); `saveDrafts` self-heilt bei Quota.
- **Inspector ist heute synthetisch** (Overpass + Fake-Hotspots `generateHotspots`), entkoppelt vom committeten Netz — wird in **P4** abgelöst, nicht vorher gelöscht.
